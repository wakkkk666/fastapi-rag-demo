# Copy this file to .env and replace the placeholder with a newly rotated key.
# Do not commit .env.
DEEPSEEK_API_KEY=replace_with_your_rotated_key

# Optional: use the default DeepSeek compatible endpoint when omitted.
# DEEPSEEK_BASE_URL=https://api.deepseek.com
